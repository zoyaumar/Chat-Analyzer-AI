# Single pinned dependency list for the project (gap D8).
# Install: pip install -r requirements.txt
#
# The CPU wheel index must come first — a per-requirement --index-url switches pip's
# index for every line after it (gap A5).

--extra-index-url https://download.pytorch.org/whl/cpu

# --- runtime ---
fastapi==0.116.1
uvicorn==0.49.0
pydantic==2.11.7
pydantic-settings==2.1.0
python-dotenv==1.1.1
python-multipart==0.0.6

SQLAlchemy==2.0.44
asyncpg==0.31.0
alembic==1.20.0

PyJWT==2.15.0
passlib==1.7.4
bcrypt==4.0.1

transformers==4.53.0
torch==2.7.0

# --- dev / test ---
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.27.0
ruff==0.16.5
