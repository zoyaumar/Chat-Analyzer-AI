# Copy this file to `.env` in the repository root and fill in real values.
#   Windows:      copy .env.example .env
#   macOS/Linux:  cp .env.example .env
#
# `.env` is git-ignored — never commit real credentials.

# Async PostgreSQL connection string used by SQLAlchemy (chat_backend/database.py).
DATABASE_URL=postgresql+asyncpg://app:app@db:5432/chat_analyzer
# Sync URL used only by Alembic, which runs migrations outside the async engine.
DATABASE_URL_SYNC=postgresql+psycopg://app:app@db:5432/chat_analyzer

# Key used to sign JWTs. Generate a long random value, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace-me-with-a-long-random-string

# Token lifetime in minutes (read through the M1 settings object — see
# gap B6 in docs/GAPS_AND_IMPROVEMENTS.md).
ACCESS_TOKEN_EXPIRE_MINUTES=30
